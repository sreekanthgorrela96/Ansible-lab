---
collections:
  - name: community.mysql
  - name: ansible.mysql
  - name: amazon.aws
    version: ">=6.0.0"
  - name: community.aws
    version: ">=5.0.0"  # Required for community.aws.aws_ssm connection plugin
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=7.0.0"
  - name: kubernetes.core
    version: ">=3.0.0"
