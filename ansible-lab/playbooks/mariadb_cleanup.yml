---
- name: Remove MariaDB configuration
  hosts: db_servers
  become: true
  gather_facts: false

vars:
    ansible_python_interpreter: /usr/bin/python3

  roles:
    - role: "../../roles/mariadb_cleanup"
